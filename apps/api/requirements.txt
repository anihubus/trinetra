# ─── DRISHTI backend: Django REST API + Channels (WebSocket) + Celery worker ───
# The `web` and `worker` compose services share this one image, so it must carry
# BOTH the web stack AND the torch-free ML serving deps that the worker's
# `run_pipeline()` call needs. It deliberately does NOT include torch/ultralytics
# — those live in ml/requirements.txt and are only needed for training/export.

# ---- web framework ----
Django>=5.0,<5.3
djangorestframework>=3.15
django-cors-headers>=4.4
drf-spectacular>=0.27          # OpenAPI docs for the frozen contract

# ---- realtime (per-tile detection push) ----
channels>=4.1
channels-redis>=4.2
daphne>=4.1                    # ASGI server

# ---- async jobs ----
celery>=5.4
redis>=5.0

# ---- database ----
psycopg2-binary>=2.9

# ---- ML serving (torch-free) — the worker imports ml.inference.pipeline ----
onnxruntime>=1.18             # runs best_detector.onnx
numpy>=1.26
opencv-python-headless>=4.10  # cv2: Lee+CLAHE preprocess, detector decode
scikit-learn>=1.5            # unpickles calibrator.pkl (per-class Platt models)

# ---- config / uploads ----
python-decouple>=3.8
Pillow>=10.3

# ---- serving / tests ----
gunicorn>=22.0
pytest>=8.2
pytest-django>=4.8
